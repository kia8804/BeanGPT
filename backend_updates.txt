# 1. SSH into your Lightsail instance (you're already connected now)
cd /opt/beangpt

# 2. Pull the latest changes from your branch
git pull origin openai-embedding-main

# 3. Restart the backend service
sudo systemctl restart beangpt

# 4. Check the status to make sure it started correctly
sudo systemctl status beangpt